/**
 * Section visibility flags for the portfolio homepage.
 * Flip any flag to `true` to re-enable that section in the UI
 * (components stay in the repo — see README "Hidden / upcoming sections").
 */
export const features = {
  /** Projects section ("Selected Works") — id="projects" */
  showSelectedWorks: false,
  /** Testimonials section ("What Clients Say") — id="testimonials" */
  showTestimonials: false,
  /** Case Studies section — id="case-studies" */
  showCaseStudies: false,
};
